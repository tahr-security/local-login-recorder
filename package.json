{
  "name": "login-recorder-local-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 playwright install chromium",
    "setup": "npm install",
    "start": "PLAYWRIGHT_BROWSERS_PATH=0 tsx index.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.0",
    "playwright": "^1.50.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
